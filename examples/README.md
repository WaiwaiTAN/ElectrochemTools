# Example data

Small, redistributable inputs live in `data/`. Generated DRT and ECM output directories are intentionally ignored. The five-minute README examples use `data/eis_cleaned.csv` and `data/eis.z60`.
