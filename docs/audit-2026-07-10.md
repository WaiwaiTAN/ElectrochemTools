# Repository audit — 2026-07-10

The phase-one work started from commit `180930ae1bb176a8793a4de55eb1437c2e191edb` on Windows with Rust/Cargo 1.96.1. The only pre-existing working-tree change was `.gitignore`, which excluded `next_movement.md`; it was preserved.

The serial baseline passed formatting, Clippy with warnings denied, all 30 tests, documentation, and the locked all-feature release build. The audit found that computation resume only checked whether a directory existed, `clean` incorrectly exposed the same resume switch, and the older end-to-end MATLAB comparison only asserted finite diagnostics. Those findings drove the v0.1.0 phase-one work.

The fixed external reference was DRTtools commit `034d9c4c4a4916a38a0e2f10381d931ffe1981b3`. MATLAB R2023b Update 9 and Optimization Toolbox were present on the validation machine.
